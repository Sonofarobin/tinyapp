<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
        <form class="form-inline" action="/login" method="POST">
          <div class="form-group mb-2" style="margin: 0 auto">
            <% if(user && user.email !== 'undefined'){ %>
              <label>You are logged in as: <%= user.email  %></label>
              <button type="submit" class="btn btn-primary" method="POST" action="/logout" >Logout</button>
            <% } else{ %>
              <div class=“navbar-nav”>
                  <a href='/register' class='btn btn-primary active' >Register</a>
                  <a href='/login' class='btn btn-primary active' >Login</a>
                  </div>
            <% } %>
          </div>
        </form>
      </div>
    </div>
  </nav>
</header>